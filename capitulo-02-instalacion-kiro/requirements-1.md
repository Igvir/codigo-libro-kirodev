WHEN a user clicks the "New Note" button
THE SYSTEM SHALL display a form with title and content fields

WHEN a user submits a note with an empty title
THE SYSTEM SHALL display a validation error and prevent submission

WHEN a user successfully creates a note
THE SYSTEM SHALL persist it to localStorage and display it in the list
